const config = {
    basename: '',
    defaultPath: '/',
    defaultClientPath: '/interview',
    fontFamily: `'Inter', sans-serif`,
    borderRadius: 8,
    // apiUrl: process.env.REACT_APP_BACKEND_URI || 'http://localhost:9000/v1',
    apiUrl: process.env.REACT_APP_BACKEND_URI || 'https://backend-n4jmdbfb5a-uc.a.run.app/v1'
};

export default config;
